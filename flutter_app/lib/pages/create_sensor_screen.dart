import 'package:flutter/material.dart';

class CreateSensorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add New Sensor'),
      ),
      body: Center(
        child: Text('Form to add a new sensor goes here'),
      ),
    );
  }
}
